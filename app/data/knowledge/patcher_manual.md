# Patcher Protocol V10 Manual

This file is auto-generated by the File Patch Manager. It defines the current registry of supported actions and their required tags.

## Action: `audit`
**Description:** Scans the codebase for specific patterns or regular expressions.

**Required Tags:** `PATCH_ID`, `ACTION`, `PATTERN`

**Literal Tags:** `PATTERN`

### Example
```text
#ACTION: audit
#PATCH_ID: [ID]
#REGEX: true
#PATTERN:
my search pattern
#END
```

## Action: `code_cut`
**Description:** Cuts a block of code and saves it to a variable for later use.

**Required Tags:** `ACTION`, `PATCH_ID`, `FILE`, `COMMENT`, `VAR_NAME`, `CONSUMER_ID`, `RANGE_START`, `RANGE_END`

**Literal Tags:** `RANGE_START`, `RANGE_END`, `REPLACE`

### Example
```text
#ACTION: code_cut
#PATCH_ID: [ID]
#FILE: path/to/file.php
#COMMENT: [Description of code being cut]
#VAR_NAME: MY_VAR
#CONSUMER_ID: [ID]
#RANGE_START:
function old() {
#RANGE_END:
}
#END
```

## Action: `code_delete`
**Description:** Deletes a block of code between two precise anchors.

**Required Tags:** `PATCH_ID`, `ACTION`, `FILE`, `COMMENT`, `DELETE_START`, `DELETE_END`

**Literal Tags:** `DELETE_START`, `DELETE_END`, `REPLACE`

### Example
```text
#ACTION: code_delete
#PATCH_ID: [ID]
#FILE: path/to/file.php
#COMMENT: [Description of deletion]
#DELETE_START:
// start anchor
#DELETE_END:
// end anchor
#END
```

## Action: `download_file`
**Description:** Downloads a file from a URL to the specified file path.

**Required Tags:** `PATCH_ID`, `ACTION`, `FILE`, `REPLACE`

**Literal Tags:** `REPLACE`

### Example
```text
#ACTION: download_file
#PATCH_ID: [ID]
#FILE: path/to/destination.md
#REPLACE:
https://example.com/file.md
#END
```

## Action: `download_icon`
**Description:** Downloads an SVG icon from the Lucide CDN to the specified file path.

**Required Tags:** `PATCH_ID`, `FILE`, `ICON_NAME`

**Literal Tags:** ``

### Example
```text
#ACTION: download_icon
#PATCH_ID: [ID]
#FILE: app/data/icons/camera.svg
#ICON_NAME: camera
#END
```

## Action: `edit_log`
**Description:** Appends a summary of changes to the system edit log.

**Required Tags:** `PATCH_ID`, `FILE`, `REPLACE`

**Literal Tags:** `FIND`, `REPLACE`

### Example
```text
#ACTION: edit_log
#PATCH_ID: [ID]
#FILE: app/data/edit-log.json
#COMMENT: [Summary of changes]
#REPLACE:
Summary of changes here.
#END
```

## Action: `file_copy`
**Description:** Duplicates a file from one path to another.

**Required Tags:** `PATCH_ID`, `ACTION`, `FILE`, `DEST_FILE`

**Literal Tags:** ``

### Example
```text
#ACTION: file_copy
#PATCH_ID: [ID]
#FILE: path/to/source.php
#DEST_FILE: path/to/destination.php
#END
```

## Action: `file_create`
**Description:** Creates a new file with the specified content.

**Required Tags:** `PATCH_ID`, `ACTION`, `FILE`, `COMMENT`, `REPLACE`

**Literal Tags:** `REPLACE`

### Example
```text
#ACTION: file_create
#PATCH_ID: [ID]
#FILE: path/to/new_file.php
#COMMENT: [Description of new file]
#REPLACE:
<?php
// new content
?>
#END
```

## Action: `file_delete`
**Description:** Deletes a file or directory.

**Required Tags:** `PATCH_ID`, `ACTION`, `FILE`

**Literal Tags:** ``

### Example
```text
#ACTION: file_delete
#PATCH_ID: [ID]
#FILE: path/to/file.php
#END
```

## Action: `file_export`
**Description:** Exports the full content of a file.

**Required Tags:** `PATCH_ID`, `FILE`

**Literal Tags:** ``

### Example
```text
#ACTION: file_export
#PATCH_ID: [ID]
#FILE: path/to/file.php
#END
```

## Action: `file_export_skeleton`
**Description:** Exports the structural skeleton (signatures, variables, bridge methods) of a file.

**Required Tags:** `PATCH_ID`, `FILE`

**Literal Tags:** ``

### Example
```text
#ACTION: file_export_skeleton
#PATCH_ID: [ID]
#FILE: path/to/file.php
#END
```

## Action: `file_move`
**Description:** Moves or renames a file.

**Required Tags:** `PATCH_ID`, `ACTION`, `FILE`, `DEST_FILE`

**Literal Tags:** ``

### Example
```text
#ACTION: file_move
#PATCH_ID: [ID]
#FILE: path/to/old.php
#DEST_FILE: path/to/new.php
#END
```

## Action: `file_overwrite`
**Description:** Overwrites or creates a file with the specified content.

**Required Tags:** `PATCH_ID`, `ACTION`, `FILE`, `COMMENT`, `REPLACE`

**Literal Tags:** `REPLACE`

### Example
```text
#ACTION: file_overwrite
#PATCH_ID: [ID]
#FILE: path/to/file.php
#COMMENT: [Description of overwrite]
#REPLACE:
new content
#END
```

## Action: `file_update`
**Description:** Standard find and replace for modifying existing code.

**Required Tags:** `ACTION`, `PATCH_ID`, `FILE`, `COMMENT`, `FIND`, `REPLACE`

**Literal Tags:** `FIND`, `REPLACE`

### Example
```text
#ACTION: file_update
#PATCH_ID: [ID]
#FILE: path/to/file.php
#COMMENT: [Description of change]
#FIND:
old code
#REPLACE:
new code
#END
```

## Action: `logic_trace`
**Description:** Verifies the existence of a code block without modifying it.

**Required Tags:** `PATCH_ID`, `FILE`, `FIND`, `REPLACE`

**Literal Tags:** `FIND`, `REPLACE`

### Example
```text
#ACTION: logic_trace
#PATCH_ID: [ID]
#FILE: path/to/file.php
#COMMENT: [Description of logic point to verify]
#FIND:
code to verify
#REPLACE:
code to verify
#END
```

## Action: `refactor`
**Description:** Launches Search and Replace studio for bulk refactoring.

**Required Tags:** `PATCH_ID`, `ACTION`, `FIND`, `REPLACE`

**Literal Tags:** `FIND`, `REPLACE`

### Example
```text
#ACTION: refactor
#PATCH_ID: [ID]
#FIND:
old pattern
#REPLACE:
new pattern
#END
```

## Action: `var_patch`
**Description:** Modifies the contents of a previously cut variable before it is used.

**Required Tags:** `PATCH_ID`, `ACTION`, `VAR_NAME`, `COMMENT`, `FIND`, `REPLACE`

**Literal Tags:** `FIND`, `REPLACE`

### Example
```text
#ACTION: var_patch
#PATCH_ID: [ID]
#VAR_NAME: MY_VAR
#COMMENT: [Description of variable modification]
#FIND:
old var text
#REPLACE:
new var text
#END
```

## Action: `var_refactor`
**Description:** Performs surgical find/replace on a cut variable (useful for renaming vars or fixing indentation during moves).

**Required Tags:** `PATCH_ID`, `ACTION`, `VAR_NAME`, `COMMENT`, `FIND`, `REPLACE`

**Literal Tags:** `FIND`, `REPLACE`

### Example
```text
#ACTION: var_refactor
#PATCH_ID: [ID]
#VAR_NAME: MY_CODE_BLOCK
#COMMENT: [Description of refactor]
#FIND:
old_variable_name
#REPLACE:
new_variable_name
#END
```

